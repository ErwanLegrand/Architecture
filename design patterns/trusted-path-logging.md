# Trusted-path logging

Write log entries through a channel outside the model's authority, to storage the model cannot reach or modify. The path from model output to committed log entry passes only through code in the trust kernel.
