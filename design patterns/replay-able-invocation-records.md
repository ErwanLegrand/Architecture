# Replay-able invocation records

Capture enough state — model identity and version, sampling parameters, full input as the model saw it, prompt template version — that any invocation can be deterministically re-executed from its log entry for forensic analysis or debugging.
